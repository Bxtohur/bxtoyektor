# Core UI
PySide6>=6.6

# Rendering
PyMuPDF>=1.24        # fitz — render PDF ke gambar

# Data
openpyxl>=3.1        # baca .xlsx multi-sheet
pandas>=2.0          # bantu normalisasi (opsional tapi dipakai)

# Search
rapidfuzz>=3.0       # pencarian toleran typo

# Dev
pytest>=8.0
ruff>=0.5

# Google Sheets (Fase 2 — opsional, aktifkan bila diperlukan)
# google-api-python-client>=2.0
# google-auth>=2.0
# google-auth-oauthlib>=1.0
